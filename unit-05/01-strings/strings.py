"""

Name:

Practice: strings

see README.md for notes

"""

# ----------------------------------------------------------------------------
# a.) Create a string and iterate over it, printing one character at a time


# ----------------------------------------------------------------------------
# b.) Write a boolean method that determines if a string is a palindrome
# (same forward as backwards). Ignore spaces and capitalization
# (e.g. "Mom", "Race car"). Test this function


# ----------------------------------------------------------------------------
# c.) Use string replace() to get a copy of a string with all occurrences of
# <name> replaces with your name, e.g. "One day <name> went to the store ..."
#
# Hint:
#   - consider the string "One day <name> went to the store"
#   - Use string find to find the index of "<name>"
#   - Use slicing to get "One day" and "went to the store"
#   - Use string concatenation to create a new string that is equal to
#     beginning + yourname + end


# ----------------------------------------------------------------------------
# d.) Use string find(), slicing, and concatenation to accomplish the task in
# part c. Do not worry about multiple occurrences (unless you want a challenge)


# ----------------------------------------------------------------------------
# e.) use string split() to get a list of words from a sentence, then use a
# loop to print each word
